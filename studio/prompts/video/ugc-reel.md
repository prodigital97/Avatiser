# UGC REEL A — "the show" (9:16, {{deliverable.duration_s}}s) — {{product.name}}

FIRST FRAME: approved creator-with-product still (image-to-video). Face =
locked identity {{character.identity.reference_images}}, zero drift.

{{scene.ugc_variant.look}} — this must feel filmed, not produced. 30fps,
auto-exposure that visibly adjusts once when they move.

Beat sheet:
- 0-2s: creator mid-sentence to camera (energy: {{character.performance.energy}}),
  raises {{product.name}} into frame — natural grip per {{product.usage_gesture}}
- 2-5s: turns product to label ({{product.label.must_read_text}} readable ~1s),
  one signature gesture from {{character.performance.gestures}}
- 5-{{deliverable.duration_s}}s: uses it — {{product.contents_behavior}} real
  physics on real skin/surface — small satisfied reaction, not a grin

Handheld truth: frame sways with breathing, one small refocus, background
window near-clipped. Their relationship to product:
{{character.performance.relationship_to_product}}.

{{realism_core}}

# CINEMATIC HERO FILM (16:9, {{deliverable.duration_s}}s) — {{product.name}}

FIRST FRAME: the approved hero keyframe render (image-to-video). The film
inherits its light, grade and composition — the keyframe already obeys the
scene sheet.

One shot, one move: slow 20-30cm dolly-in toward {{product.name}}, 24fps,
shutter 1/48, {{scene.camera.video_rig}}.

Motion inside the frame (pick what the keyframe set up):
- atmosphere drifting through the key light ({{scene.settings.primary.light.key}})
- {{product.contents_behavior}} in real physics — liquid settles with
  meniscus, never loops unnaturally
- fabric/foliage from {{scene.settings.primary.set_dressing}} breathing once

The product itself moves LAST and least — a highlight sliding across
{{product.finish_behavior}} as the camera closes. Label
({{product.label.must_read_text}}) stays exact and legible; if motion would
warp it, reduce the move, not the fidelity.

End on a settled frame that could hold a logo for 1s. No speed ramps, no
direction changes, no orbit.

{{realism_core}}

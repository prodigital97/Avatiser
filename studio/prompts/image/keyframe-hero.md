# HERO KEYFRAME — {{product.name}}

REFERENCE IMAGES: {{product.label.reference_images}} (label fidelity is
non-negotiable — reproduce the referenced packaging exactly).

Cinematic hero frame of {{product.name}} ({{product.form_factor}}) in
{{scene.settings.primary.location}}, {{scene.settings.primary.time_of_day}}.

Product at its hero angle: {{product.hero_angle}}. Scale true to
{{product.dimensions}}. Materials behave as they are: {{product.finish_behavior}}.

LIGHT — {{scene.settings.primary.light.key}}; fill {{scene.settings.primary.light.fill}}.
Every highlight on the product must trace back to this source.

CAMERA — {{scene.camera.stills_rig}}. Product in the focus plane, background
falls off naturally. Composition leaves negative space for a logo lockup
(rule of thirds, product off-center).

SET — {{scene.settings.primary.set_dressing}}. Include imperfections:
{{scene.settings.primary.imperfections}}.

GRADE — palette {{scene.camera.palette}}, {{scene.camera.grade}}.

This frame becomes the first-frame reference for the hero film — compose it
as a shot that wants to move (a beam to drift through, fabric to settle).

{{realism_core}}

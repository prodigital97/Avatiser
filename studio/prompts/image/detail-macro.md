# DETAIL MACRO (1:1) — {{product.name}}

REFERENCE: {{product.label.reference_images}}.

Extreme close-up of {{product.name}}'s most tactile zone (cap thread, texture
of contents, embossed logo, nozzle). 100mm macro at f/5.6 — focus plane a few
millimetres deep, everything else melts.

Contents/material truth: {{product.contents_behavior}};
{{product.finish_behavior}}. Show micro-reality: machining marks, surface
grain, a fingerprint ghost near where thumbs go.

Light: single hard source raking across the surface to carve texture
({{scene.settings.primary.light.key}} direction, intensified). Palette held
to {{scene.camera.palette}}.

The frame should feel like evidence of manufacturing quality — the shot a
skeptical buyer zooms into.

{{realism_core}}
